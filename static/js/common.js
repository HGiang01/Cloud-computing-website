const BASE_URL = window.location.origin;

// User
var userListUrl = `${BASE_URL}/getUsers`
var userInsertInsertUrl = '${}/insertUser'

// Product
var productListUrl = `${BASE_URL}/getProducts`
var productUpdateUrl = '${}/updateQuantity'

// Order
var ordersListUrl = `${BASE_URL}/getOrders`
var insertOrder = `${BASE_URL}/insertOrder`

// Page
var groceryPage = `${BASE_URL}/grocery`

// Login
var login = `${BASE_URL}/login`

// Logout
var logout = `${BASE_URL}/logout`